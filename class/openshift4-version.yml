parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-version/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - openshift4-version/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-version/
